# Open Campus Achievement (OCA) API Key
# This is used for issuing achievements to users
OCA_API_KEY=your_api_key_here

# Environment - set to "sandbox" or "production"
OCA_ENVIRONMENT=sandbox

# Image URL for credentials (defaults to public EduHub image if not set)
CREDENTIAL_IMAGE_URL=https://example.com/your-logo.png 